# Secrets — never commit API keys (see .env.example for the expected shape)
.env
.env.*
!.env.example

# Generated art/audio output (large binaries reviewed before curated copies land in-repo)
tools/asset-gen/out/

# OS noise
.DS_Store
